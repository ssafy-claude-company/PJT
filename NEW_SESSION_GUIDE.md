# 새 세션 시작 가이드 (NEW_SESSION_GUIDE)

> 새 Claude 세션의 **첫 마디**: **"PJT의 NEW_SESSION_GUIDE.md 읽고 시작해."**
> 이 가이드는 '처음 5분에 할 일'과 '지켜야 할 것'만 담는다. 깊은 맥락(구조 결정
> 이력·자격증명·복구 원리)은 [SESSION_HANDOFF.md](SESSION_HANDOFF.md)를 이어서 읽는다.

## 0. 이 시스템이 뭔가
Discord 멀티에이전트 AI 회사 **"Organt"** — 사용자가 Discord로 일을 시키면 봇 직원들이
팀을 꾸려 협업(위임·회의·표결·검증)으로 작품을 만들고 배포까지 한다.
- 레포 2개: `PJT`(코드, `/home/user/PJT`) + `docs`(설계 문서, `/home/user/docs`)
- 작업 브랜치: 세션마다 발급되는 `claude/*` — **main이 통합 기준**(브랜치 푸시 후 main FF
  동기화 관행). 시작 전 `git fetch origin main`으로 다른 세션의 선행 커밋을 확인하라.
- 설계 기준은 docs 레포의 Rule — 코드가 Rule과 다르면 코드가 버그다.

## 1. 처음 5분 — 부팅 체크리스트 (순서대로)
```bash
cd /home/user/PJT
# ① 리스너 생존 + 단일 인스턴스 — 정확히 1이어야 한다(2면 게이트웨이 복제 사고)
P="src"; pgrep -fc "python -m ${P}.main"
# ② ready 확인 — "User 입력 대기 중"이 보이면 정상
tail -5 logs/listener.log
# ③ 죽어 있으면 재기동 — 반드시 이 래퍼(필수 env 주입). 로그 리다이렉션 잊지 말 것(stdout 로그).
setsid nohup bash scripts/run_listener.sh >> logs/listener.log 2>&1 < /dev/null &
```
- ③-보충: 영속 env에 풀팀 토큰(`SYSTEM_BOT`+`ORGANT_BOT_2~20`+`TEST_BOT_1`(=1번 봇))과 배포 키(GH_PAT·RENDER_KEY)가
  등록돼 있어 `.env` 없이도 **풀팀(워커 20)으로 뜬다**(2026-06-12 새 컨테이너에서 라이브 검증).
  새 컨테이너면 `.venv`·`logs/`부터 만들어야 한다(`python3 -m venv .venv` + requirements 2종).
- ④ **감독자 무장** — 분리 프로세스(세션이 끝나도 컨테이너가 살면 생존, 평시 토큰 0).
  **멱등이라 그냥 실행하면 된다** — 이미 떠 있으면 flock이 거부한다('딱 1개' 구조 보장):
  ```bash
  setsid nohup bash scripts/supervisor.sh >> logs/supervisor.log 2>&1 < /dev/null &
  ```
  사건 기록은 `logs/supervisor.log`(무장/재기동/ready 한 줄씩). 세션 Monitor 방식은
  persistent 지정에도 30분 캡으로 죽어 폐기됨(핸드오프 항목 28).
- ⑤ 기준선 확인: `source .venv/bin/activate && python -m pytest -q` → **전부 통과**가 기준선
  (개수는 늘어나는 스냅샷이라 박지 않는다 — 실행 결과가 진실원).

## 1.5 주요 환경변수 — 처음 보면 오해하기 쉬운 것들
| env | 정체 | 주의 |
|---|---|---|
| `SYSTEM_BOT` | 시스템 봇 토큰(라우팅·상태 메시지 명의, id 1510856525240078456) — **직원 아님** | 영속 env |
| `ORGANT_BOT_2~20` | 워커 봇 토큰(testtest2~20) — 등록된 만큼 합류(로스터 자동 적응). **`ORGANT_BOT_1` 변수는 없다**(1번 봇은 아래 TEST_BOT_1) | 영속 env(풀팀) |
| `TEST_BOT_1` | **1번 봇** = 워커 계정 시리즈 1번(username `testtest`, id 1510828738181595156) — 현 게임 비주얼 디자이너 | 영속 env |
| `TEST_OBT_2/3` | 구세대 별칭 — **같은 1기 봇들의 옛 키**(OBT_3=ORGANT_BOT_2와 동일 봇=김민준, 현 **백엔드**·To 미지정 폴백 담당). 새 영속 env엔 없음. 같은 봇의 키 둘을 로스터에 동시 주입 금지 | 레거시 |
| `CHANNEL_ID` | **메인 채널(#test)의 ID**(기본 1510828120490643517) — 서버(길드) ID가 아니다. 단, 길드 ID를 넣어도 `channels.py`가 안에서 대상 채널을 해석(오인 흡수 설계) | |
| `DEPLOY_NAME` | **폐지된 유물**(2026-06-12) — 코드가 더 이상 읽지 않는다. 배포 슬롯은 P-번호로만(`organt-p-00n`), **미등록 흐름은 배포 불가**(create_project 등록이 먼저) | env 삭제 무방 |

직군의 진실원은 `jobs.json` > Discord 역할 > 로스터 폴백 라벨 순 — 로스터의 라벨은 참고일 뿐이다.

## 2. 불변 방향성 (사용자 확정 — 재논의하지 말 것)
- **메타 목표(모든 원칙의 상위)**: 구조화된 대화로 **최적의 대화만** 해서, 토큰을 과소비하지
  않고도 **품질적으로 최고의 제품**을 만드는 정밀 설계 아키텍처. **둘이 충돌하면 품질이
  우선이다**(사용자 확정) — '최적의 대화'란 대화를 줄이는 게 아니라 다시 하지 않아도 되는
  대화다(잘린 보고·재질문·검증 생략·사후 재작업이 진짜 낭비). 아래 원칙들은 이 목표의 수단이며,
  **범용적·대화적 이치에 맞는 제한은 하드 제한이어도 옳은 제한**이다(개별 원칙 문구에 과잉 구속되지
  말 것 — 단, 특정 도메인/직군 분기 하드코딩은 여전히 금지).
  **판단 기준은 '정량/하드냐'가 아니라 '어느 상황에서든 보편적으로 이로운 이치냐'다**(사용자 재확인
  2026-06-13). ✅ 보편 이치 → 하드·정량이어도 박는다(교차 검증 의무, 침묵 절단 금지 — 제3자 검증·
  내용=산출물은 어디서나 참). ❌ 본질 아닌 임의 임계(매직넘버) → 뺀다(예: 'goal 4항목+이면 분해'는
  틀림 — 분해의 본질은 개수가 아니라 '독립적으로 검증할 단위인가'. '6명+ 팀 안내'도 같은 점검 대상).
  즉 '하드코딩 금지'를 '정량 금지'로 오해하지 말 것 — 임의값이 문제지 하드함이 문제가 아니다.
- **작동≠좋음, 창의는 "창의적으로 해"로 안 나온다(RFC-010, 연구 근거 — 잊지 말 것)** — 정렬(RLHF)이
  LLM을 '전형·자명한 1개 완성'으로 수렴시켜(mode collapse) **"언급한 것만"** 구현하게 만든다(라이브:
  배포 게임이 마법 1개·가이드 정보덤프·노잼). 품질은 프롬프트가 아니라 **구조로 우회**한다: ① 검증은
  **코드 읽기 아닌 실제 실행·플레이 + '재밌나·뭐가 아쉽나·더 좋은 버전엔 뭐가' 비평**(TITAN 실플레이
  82%>읽기 18%) ② **비평자는 만든 사람 아닌 다른 멤버**(자기검증은 편향만 키움 — Pride&Prejudice)
  ③ **발산→수렴**(자명한 1개 말고 복수안 비교) ④ **완성=작동이 아니라 '써보니 좋다'**(작동≠좋음,
  Pirsig·Craftsmanship). **천장(정직 — RFC-011에서 방향 교정됨)**: LLM이 *진공*에서 자기 산출을
  판정하면 취향 천장(~0.5)에 막힌다. 그러나 천장을 넘는 길은 **사람을 검증자로 넣는 게 아니라 판정을
  외부 현실에 묶는 것** — 자기 출력을 실제로 보고(비전)·실제 레퍼런스와 대조(검색)·객관적 완성 점검.
  **사용자는 검증 게이트가 아니다**(사용자 지적 2026-06-15 "검증을 왜 날 포함하나, 목표를 잊었나").
  목표는 **사용자 없이 상용 품질을 내는 자율 회사**. (P1~P5가 게이트 문구에 박혀 있음.)
- **상용 품질 = 닿는 자원 다 끌어다 자율로, 사용자 요구 0 (RFC-011, 사용자 2026-06-14~15)** —
  '작동하지만 상용 아님'(이펙트·캐릭터·BGM·기본공격 저품질)의 근본은 ① 구조가 *LLM이 싸게 검증 가능한
  것*(presence·무크래시)으로 수렴 + ② 취향 천장. 처방 3겹: **M1 현실기준**(워커에 WebSearch/WebFetch —
  '훌륭한 예'를 상상 말고 *검색해 대조*) · **M2 체험·비전 검증**(presence는 '좋음'의 증거 아님을 명시 +
  시각 산출물이면 스크린샷→Read로 '눈으로 보고' + 레퍼런스 대조) · **M3 취향 축적**(사용자 채널 비평을
  `projects.json`에 누적→set_goal·검증에 주입 — 단 *상시 게이트 아닌 bootstrap*, 외부현실 기준에 종속).
  **자원동원 원칙**(`_base_work_note`): 어떤 차원이든 placeholder를 최종으로 내지 말고 WebSearch로 실제
  무료(CC0) 자원 받아 통합 · 모르는 기법은 검색해 익혀 적용 · **사용자/위임자에게 키·자원 요구 금지**.
  발견한 소스·기법은 `[경험]`→수면 증류로 영구 자산화(학습 플라이휠에 얹힘). **하드코딩 0**(특정
  URL·도메인 예시 금지 — 게임 프라이밍은 RFC-010·011에서 두 번 제거됨). **생성능력 통찰**: 'X 직군이
  없어 못 만든다'가 아니다 — 그 직군도 LLM이라 코드로 합성할 뿐(라이브: 사운드=프론트가 오실레이터로
  합성한 삑음, 사운드 직군 0·recruit 0). 진짜 품질은 **실제 에셋을 받아와야** 나온다(컨테이너 아웃바운드
  열림 실측: kenney·freesound·HF 200). **⚠ 미검증**: 이 3겹은 *프롬프트*만 섰고 런타임 발화는 미확인 —
  §6 참조.
- **구조적 보장 > 프롬프트/LLM 판단** — 기계적 행동은 시스템이 하고, LLM에게는 판단만 맡긴다.
- **단일 호출·단일 흐름**(request→response, 단일활성 베턴)은 의도된 설계(퀄리티·토큰 안정성).
  병렬은 '서로 다른 프로젝트의 흐름' + fork(독립 의견 동시 수집) + **독립 Work 동시 실행**
  (parallel_work — 파일 영역 분리 2~3건, 조인·통합·판정은 직렬. RFC-006, 사용자 승인 대개편)
  — 봇은 전역 점유(한 시점 한 흐름). "순차적인 일은 순차, 병렬이 필요한 부분만 병렬."
- **1봇 1직업 1기억**(전문화). 범용 직군(풀스택·제너럴 등)은 폐지 — recruit가 정책 거부.
  직군 밖 Work는 받은 전문가가 보고 첫 줄 `[직군밖] 필요직군`으로 반려 → 리더가 채용.
- **시스템 내부 데이터의 Discord 노출 금지** — 채널의 시스템 명의 메시지는 상태 메시지
  (+큐 접수 답글)뿐. 거부·표결 집계·배포 결과는 도구 반환으로 당사자에게만.
- 전 직군 **공용 메커니즘**(특정 직군 키워드 하드코딩 금지). 도메인 판정은 그 분야 전문가가.
- 프로젝트 신원은 **P-번호**(이름이 아님) — 폴더 `p-00n-*`, 배포 슬롯 `organt-p-00n`.
- 에이전트 모델: **sonnet**(`ORGANT_MODEL`).
- 사용자 확인 게이트를 늘리지 말 것 — 목적은 **자동 회사**(자율성). hook=개인 권한 제한일 뿐.

## 3. 운영 수칙
- 커밋 규약: **`type(scope): 한글 제목`** + 본문에 왜·라이브 근거. 변경은 작게.
- 푸시: 브랜치 → **main FF 동기화까지 자율**(확립된 관행). PR은 사용자가 시킬 때만.
- 비밀(봇 토큰·GH_PAT·RENDER_KEY)은 gitignored `.env`에만 — **절대 커밋 금지**.
- 라이브 리스너가 도는 중 `logs/*.json` 직접 수술 금지(쓰기 충돌) — 멈추고 수술하고 재기동.
- 재기동 전 흐름 진행 여부 확인: 봇 CLI(`_bundled/claude`) 프로세스 + logs 파일 mtime.
  (`/tmp/restart_organt.sh`가 있으면 그걸 사용 — 능동 흐름 가드 내장, `--force`로 무시.)
- `pkill`/`pgrep`은 **분할 패턴**(`P="run_"; pkill -f "${P}listener.sh"`) — 자기매칭(exit 144) 회피.
  **인자만 분할하면 부족**: 같은 커맨드라인 다른 곳의 리터럴(예: 뒤따르는 재기동 명령)도 매칭된다 —
  pkill과 재기동은 **호출을 분리**하라(2026-06-12 실증: 한 커맨드라인에 합쳤다가 자기 셸·감독자 동반 사살).
  감독자는 이제 분리 프로세스라 면역(argv에 스크립트 경로뿐 — 파일 내용은 pkill -f 매칭 대상 아님).
- 감시는 **감독자 1개**(flock이 구조 보장) — 유휴 시간대의 세션 Monitor류 상시 감시는 금지(토큰 누수).
  **예외 1개: '작업 중 하트비트'** — 흐름이 도는 동안만 타임아웃 ~8분 Monitor를 1개 무장한다
  (컨테이너 생존용 — 사용자 확인 2026-06-12 '하트비트는 살려두라'. 동면 임계 실측 유휴 3~8분).
  목적은 감시가 아니라 **깨우기**: 동면은 박스 안 코드로는 못 막고, 박스 밖(하니스) 타임아웃
  알림만이 잠든 박스를 되살린다. 깨어나면: 흐름이 돌면 점검 후 재무장, 유휴면 무장하지 않는다
  (부팅 복구·졸업 라우팅이 피해 0). 비용: 작업 중에만 시간당 ~7회 짧은 깨움(컨테이너 생존 대가).
  ※ '인간 방식에서 장점만 증류' 원칙(핸드오프 38)은 **에이전트 협업 구조**에 대한 것 —
  봇은 일이 있을 때만 깨운다(상시 발화하는 뉴런을 복제하지 않음), 병렬은 토큰 중립 조건에서만.
- **침묵 한계 금지** — 내용을 자르거나 한도가 발동하면 반드시 그 자리에 표기를 남긴다(`_speech_clip`
  패턴: "자를 수는 있어도 조용히는 못 자른다"). `[:N]` 슬라이스는 라벨·로그 꼬리에만 — 발언·근거·
  회의록처럼 **내용이 곧 산출물**인 데이터에 쓰면 협업이 침묵 속에 부서진다(라이브: 회의 전원
  "…프론트엔" 동강, 동강난 주장으로 토론 — 사용자가 발견할 때까지 어떤 로그에도 안 보였음).
- **계기판은 죽으면 죽어 보이게** — 상태·시각 표기는 스스로 늙는 형식(Discord `<t:유닉스:R>`)으로.
  갱신 시점에 계산한 'N초 전' 고정 문자열은 시스템이 멈추면 박제돼 **거짓 생존 신호**가 된다.
- **검증한 범위만 보증** — 문서·주석·반환문에 "위험 없음/자가 치유"류 단정은 실제 검증한 케이스로
  한정해 쓴다(라이브 2건: "중복 실행 위험 없음"이 미완-재발사 사고를, "자가 치유"가 복구-재시작
  오해를 가림). 한계를 아는 만큼 적는 것이 인수인계다.

## 4. 진단 빠른 참조
| 무엇 | 어디 |
|---|---|
| 흐름 이벤트(시작·마감·거부·반려) | `logs/flow.jsonl` |
| 봇 도구 호출 전수 | `logs/audit.jsonl` |
| 리스너 stdout(부팅·수신·에러) | `logs/listener.log` |
| 프로젝트 레지스트리(P-001~) | `logs/projects.json` |
| 직군 기억 | `logs/jobs.json` (진실원: jobs.json > Discord 역할 > 로스터) |
| 직무 기준·경험(학습 플라이휠) | `logs/role_profiles.json` |

- 알려진 외부 충격(정상 범주): **컨테이너 회수/동면**(프로세스 전멸, `/home`은 보존) →
  감독자 + 부팅 복구가 자가 치유. 복구 규칙: 미응답 [Request]는 재처리하되, **이미 프로젝트로
  졸업한 원요청은 재발사하지 않고 그 프로젝트 채널 '개입'으로 잇는다**(기억·미완 Task 유지 —
  핸드오프 29). **Render free 슬립**(15분 유휴, 첫 접속 ~1분 콜드스타트)은 결함이 아니다.
- 협업 품질 분석을 요청받으면: flow.jsonl(이벤트 시퀀스) + 채널 스레드(발언 명의) +
  audit.jsonl(실작업)로 교차 확인 — 중앙집권 경향(협의 없는 일방 결정)도 함께 본다.
- **분석은 '일어난 일'만 보면 두 부류를 놓친다**(라이브 실증 — 잘림·회의 부재 둘 다 사용자가
  먼저 발견): ① **부재** — 회의·표결·교차리뷰가 0건이면 이벤트가 없어 로그에 안 보인다.
  "이 규모면 있어야 할 것"의 기대 기준을 세우고 0건을 질문으로 다뤄라. ② **내용 훼손** —
  스레드 최소 1개는 grep이 아니라 **독자로서 전문 통독**(발언이 문장으로 완결되는지, 동강·
  박제·중복이 없는지). 설계 적합성 검사는 잘못된 설계를 통과시킨다 — 결과물을 사람처럼 읽어라.

## 5. 하지 말 것
- 사용자 요청 없는 PR 생성 / 모델 ID를 커밋·코드·문서에 기록(채팅 답변에서만).
- '이름'으로 프로젝트 식별·작업공간 추측 — 신원은 P-번호, 작업공간은 레지스트리에서 읽는다.
- 배포 서비스명 수동 지정 — 슬롯은 `deploy_service_name`이 pid로 결정한다.
- 특정 직군 전용 하드코딩 게이트 추가 — 공용 메커니즘 + 전문가 자기정의가 원칙.
- 같은 요청을 봇에게 재시도 폴링 — 점유·대기는 시스템이 안내한다.

## 6. 지금 열린 것 (2026-06-15 — 우선순위 순)
> **이 세션의 가장 큰 교훈**: RFC-011(M1/M2/M3·자원동원)은 **구조(프롬프트)만 섰고 런타임에서 실제
> 발화하는 걸 본 적이 없다.** '지었다'를 증명 없이 반복하지 말 것 — 다음은 *더 짓기*가 아니라 **실증**이다.
- **#3 실증 (최우선)** — 한 흐름 돌려 **WebSearch·실제 에셋 다운로드·비전검증·자원동원**이 실제로
  발화하는지 트레이스 확보. 라이브 리스너엔 사용자처럼 메시지를 못 넣으니(봇/시스템 발화 무시) 트리거는
  (a) 사용자가 프로젝트 채널에 작업 투입 시 추적, (b) 리스너 잠깐 내리고 통제된 1회 흐름 직접 구동.
  **진짜 품질은 결국 사람이 플레이/사용해 봐야** 보인다(천장) — 단 그건 *검증 게이트*가 아니라 *판정 확인*.
- **#4 체계화 (실증 후)** — `set_goal` 확정 반환이 5겹 넛지 `{tip}{qbar}{creative}{gapcheck}{taste}`를
  매번 붙인다(guide_tools.py). 적층이 LLM 주의를 분산 → **하나의 품질 기준으로 통합**. 단 실증으로 뭐가
  실제 작동/무용한지 본 뒤(헛 통합 방지).
- **M3 긴장** — M3가 사용자 취향을 검증 입력으로 쓰는 건 '자율·사용자 0' 목표와 반쯤 모순. 외부현실
  (레퍼런스·비전) 기준에 *종속*되게 정리 필요.
- **운영 주의** — 컨테이너 **리클레임 시 감독자·리스너 둘 다 죽는다**(감독자는 '세션 종료'엔 생존하나
  '컨테이너 재시작'엔 안 죽음 ← 이번 세션 ~5회 관측). 새 세션은 부팅 체크리스트(§1)로 둘 다 살릴 것.
- (이월) **[직군밖] 반려·범용 채용 거부 라이브 발동**, **P-005 채널 404 레지스트리 정리** — 미해결.
